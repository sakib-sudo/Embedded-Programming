#include<stdio.h>
void swap(int*x, int*y){
int temp = *x;
*x = *y;
*y = temp;
}
int main(){
printf("Before swap: a = %d, b = %d");
swap(&a, &b);
printf("After swap : a = %d, b = %d", a, b);
return 0 ;
}
